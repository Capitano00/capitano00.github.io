---
import { disclosureRules } from "@/data/site";
---

<section aria-labelledby="disclosure-heading" class="mx-auto max-w-7xl px-5 py-16 sm:px-8">
  <div class="grid gap-8 border-y border-copper/25 py-8 md:grid-cols-[0.72fr_1.28fr]">
    <div>
      <p class="text-sm font-semibold uppercase tracking-[0.16em] text-copper">Publication guardrails</p>
      <h2 id="disclosure-heading" class="mt-3 font-display text-3xl font-semibold leading-tight text-ink md:text-4xl">
        Designed for colleagues, board readers, and public LinkedIn visitors
      </h2>
      <p class="mt-4 leading-7 text-ink-soft">
        These rules are intentionally visible because the boundary is part of the product story.
      </p>
    </div>
    <div class="grid gap-3 md:grid-cols-2">
      {
        disclosureRules.map((rule) => (
          <p class="rounded-md border border-ink/10 bg-white/72 px-4 py-3 text-sm leading-6 text-ink-soft">{rule}</p>
        ))
      }
    </div>
  </div>
</section>
